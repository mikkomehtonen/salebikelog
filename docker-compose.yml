services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads:Z
      - ./database:/app/database:Z
    environment:
      - LM_STUDIO_URL=http://host.docker.internal:1234
      - LM_STUDIO_MODEL=qwen/qwen3.6-35b-a3b
      - DB_PATH=/app/database/trips.db
